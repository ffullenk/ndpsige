<fieldset class="item">
	
	<%= f.association :producto, :label_method=> :nombre, :collection=> @productos,:prompt =>"Seleccione"   %>
	<%= f.input :cantidad, :as => :string, :label=> "Cantidad",:hint=> "#{link_to 'Eliminar', :html=> {:class=> 'remove' }}" %>
	<%= f.hidden_field :id %>
	<%= f.hidden_field :_destroy %>
</fieldset>